// program to concatenate two strings except for their first character. 

function mergeString(str1, str2)
{
    return str1.substring(1) + str2.substring(1);
}

console.log(mergeString('mast hai','duniya'));
console.log(mergeString('a','hi'));
console.log(mergeString('','wow'));