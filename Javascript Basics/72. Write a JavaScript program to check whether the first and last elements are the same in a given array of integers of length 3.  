// program to check whether the first and last elements are the same in a given array of integers of length 3.  

function check_array(arr)
{
    return arr[0] == arr[2];
}
console.log(check_array([1,2,1]));
console.log(check_array([42,3,8]));